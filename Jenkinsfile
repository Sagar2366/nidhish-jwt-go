pipeline {
    agent any 
    stages {
        stage('Verify java is installed or not') { 
            steps {
                sh "java --version"
                sh "whoami"
            }
            }
        }
}
